# Builder answers 1

1. I don't know whether a mixed-unit column should be converted row by row or fail as a whole. Mixed-unit handling is unspecified.

2. I don't know the output naming or destination policy. The known requirements are only that the converted file be clearly named and originals remain read-only; the suffix, destination, and overwrite behavior are unspecified.
