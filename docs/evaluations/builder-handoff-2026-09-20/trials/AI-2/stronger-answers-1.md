# Stronger answers

Q1. Keep `legacy-pages` as the active page-location rule for now. The `content/` and `assets/` layout is an unapproved former proposal with no date or confirmation, so migration planning can be prepared for review but should not be treated as approved.

Q2. Agents may make local changes and run checks; the release volunteer is the stated publisher. The hosting owner and whether that volunteer is active are unknown, so no other role should be named and deployment authority should not be assumed.

Q3. Document `scripts/check-links.ps1` as the required link-check command under the current guidance. The supplied tree is synthetic, so whether it runs successfully in the real repository and the resulting evidence remain unverified.
