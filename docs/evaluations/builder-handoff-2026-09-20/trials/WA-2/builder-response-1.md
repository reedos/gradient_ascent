I inspected the supplied artifact brief and the three explicit synthetic attachments listed in `attachments.json`: `registration-export.csv`, `schedule-sheet.csv`, and `access-notes.md`. They establish one duplicate identity with conflicting phone numbers and shifts, a schedule row for the 09:00 shift, and that export/attendance editing is available while scheduling permission and reminder authority are unknown. I could not independently verify systems, permissions, volumes, timings, or the referenced public concept sources from this handoff.

The bounded recommendation is a review-gated reconciliation pilot: create a staging table from each registration export, normalize event and shift values, flag probable duplicate/conflict cases, and generate a coordinator review queue. Only coordinator-approved rows may update a copy or designated staging view of the schedule; no volunteer-facing publication, reminders, silent merges, or production workbook connection is included. Preserve source values, decision, reviewer, timestamp, and reason for every correction.

I will treat savings, match rates, and error reduction as unmeasured until the pilot records baseline and outcome evidence. The acceptance evidence should show complete intake accounting, deterministic flags for the supplied Ari Lee cases, explicit handling of unresolved rows, coordinator approval before publication, and a tested rollback to the prior schedule snapshot. Material organizational decisions remain event naming, retention, matching key, accessibility-data handling, schedule-change authority, and reminder authority.
