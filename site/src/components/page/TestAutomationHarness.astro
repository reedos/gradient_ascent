---
import Link from './Link.astro';
---
<figure class="test-harness" aria-labelledby="test-harness-caption">
  <div class="brief">
    <span class="label">Instructions + context · you provide the starting files</span>
    <div class="parts input-files">
      <div><strong>CLAUDE.md</strong><span>Reusable framework instructions: conventions, boundaries, approvals, and deliverables</span></div>
      <div><strong>DUT_BRIEF.md</strong><span>New for each DUT: requirements, differences from past projects, and known configuration</span></div>
    </div>
    <div class="harness-controls">Also provide access to framework documentation and source, past projects, and the standard template.</div>
  </div>
  <div class="flow" aria-hidden="true">↓</div>
  <div class="harness">
    <span class="label">Claude Code · agent harness</span>
    <div class="model"><strong>Claude · model</strong><span>Reads the context, asks about missing requirements, and proposes a project plan</span></div>
    <div class="plan-gate"><span class="label">Human approval · agent drafts, you approve</span><strong>PROJECT_PLAN.md</strong><span>Reference project, proposed files, intended changes, reused tools, and non-hardware checks</span></div>
    <div class="loop">Wait for your approval → generate project files → check without hardware</div>
    <div class="harness-controls">Reuse the closest project, or a template. Framework changes and new project-local tools need separate approval.</div>
    <div class="control-map">
      <span class="label">Controls around each action · intended setup</span>
      <div class="parts">
        <div><strong><Link href="/techniques/guardrails/">Guardrails</Link></strong><span>Check proposed actions and generated files. Block or flag prohibited changes; pause for required approvals.</span></div>
        <div><strong>Permissions + sandbox</strong><span>Restrict writes to the approved project and withhold live instrument access. Framework protection needs confirmation.</span></div>
        <div><strong><Link href="/techniques/observability/">Observability</Link></strong><span>Record edits, commands, approvals, check results, and blocked actions with their reasons.</span></div>
        <div><strong><Link href="/techniques/cost-optimization/">Stop controls</Link></strong><span>Pause for decisions; stop at configured limits or repeated failures and report unfinished work.</span></div>
      </div>
    </div>
  </div>
  <div class="flow"><span aria-hidden="true">↓</span> Generate project files using existing framework interfaces</div>
  <div class="framework">
    <span class="label">Shared framework + new DUT project</span>
    <div class="parts">
      <div><strong>Project conventions</strong><span>Templates, documentation, previous projects</span></div>
      <div><strong>Tools + established functions</strong><span>The agent reads and edits files. Generated code reuses measurements, units, and CSV export; SCPI stays behind framework interfaces.</span></div>
      <div><strong>Agent-generated deliverables</strong><span>Python code + YAML/JSON configuration + Markdown documentation, including PROJECT_STATUS.md</span></div>
      <div><strong>Non-hardware checks</strong><span>Python syntax and configuration validity; no instrument connections</span></div>
    </div>
  </div>
  <div class="flow" aria-hidden="true">↓</div>
  <div class="brief"><span class="label">Evaluation + human review</span><strong>Review against requirements → test on real instruments → return logs and observations</strong><div class="harness-controls">Evaluate requirements, framework reuse, and approval compliance. The agent updates PROJECT_STATUS.md with actual checks, supplied hardware results, and unresolved issues.</div></div>
  <div class="flow"><span aria-hidden="true">↺</span> Context for the next call: approved plan + relevant files + feedback → revise within scope</div>
  <figcaption id="test-harness-caption">CLAUDE.md is reused across DUTs; DUT_BRIEF.md changes for each project. The plan, brief, and status filenames are conventions for this example. Permissions are configured separately: Markdown instructions do not enforce filesystem or instrument restrictions. This is a specified workflow, not a recorded run; read-only framework access remains to be confirmed.</figcaption>
</figure>
<style>
  .test-harness { margin: 26px 0; padding: 20px; background: var(--panel); border: 1px solid var(--line); border-radius: 12px; }
  .label { display: block; color: var(--muted); font-size: 10px; text-transform: uppercase; letter-spacing: 1px; margin-bottom: 10px; }
  .brief { padding: 20px 24px; text-align: center; border: 1px solid var(--line); border-radius: 7px; }
  .test-harness strong { display: block; font-size: 15px; line-height: 1.5; color: var(--text); }
  .harness { border: 1px solid var(--o5); border-radius: 8px; padding: 18px; }
  .harness > .label { color: var(--o5); }
  .model { background: var(--panel2); border: 1px solid var(--line); border-radius: 6px; padding: 20px; }
  .input-files { text-align: left; }
  .control-map { margin-top: 20px; padding-top: 18px; border-top: 1px solid var(--line); }
  .plan-gate { border-left: 3px solid var(--o5); background: var(--panel2); padding: 18px 20px; margin-top: 18px; }
  .plan-gate > span:last-child { display: block; color: var(--muted); font-size: 14px; line-height: 1.7; margin-top: 8px; }
  .model span, .parts span { display: block; color: var(--muted); font-size: 14px; line-height: 1.7; margin-top: 8px; }
  .loop { color: var(--text); font-size: 12px; line-height: 1.8; padding-top: 14px; }
  .harness-controls { color: var(--muted); font-size: 12px; line-height: 1.7; padding-top: 8px; }
  .flow { color: var(--muted); text-align: center; padding: 10px 0; font-size: 11px; line-height: 1.6; }
  .flow > span { color: var(--o5); margin-right: 6px; }
  .framework { padding: 18px; border: 1px solid var(--o4); border-radius: 8px; }
  .framework > .label { color: var(--o4); }
  .parts { display: grid; grid-template-columns: 1fr 1fr; gap: 18px; }
  .parts > div { padding: 18px; min-width: 0; background: var(--panel2); border: 1px solid var(--line); border-radius: 6px; }
  figcaption { color: var(--muted); font-size: 11px; line-height: 1.8; margin-top: 18px; }
  @media (max-width: 550px) { .test-harness { padding: 12px; } .parts { grid-template-columns: 1fr; } .harness, .framework { padding: 13px; } }
</style>
