export const designDecisions=[
 {title:'Context, retrieval, and memory',question:'What information must be available for this request?',baseline:'Send a small, relevant source packet. Add retrieval when the collection is too large or changes too often to include directly.',distinction:'A long context window is capacity, not a relevance or accuracy guarantee. Retrieval selects information; memory persists selected information across requests. A system may use all three.',depth:'Preserve document identity, access scope, applicability dates, and source provenance. Decide what memory may be written, corrected, expired, or deleted. A summary can lose a constraint; a stale memory can contradict a current source. Test both missing evidence and misleading evidence.',measure:'Measure retrieval coverage separately from whether the answer is supported. Include questions with no answer and contradictory sources.',link:'rag',source:'https://arxiv.org/abs/2005.11401',sourceName:'RAG paper'},
 {title:'Workflow or agent?',question:'Can you specify the useful steps before the run starts?',baseline:'Use a fixed workflow when the sequence and branch rules are known. Use an agent loop when the next useful action depends on what the model discovers.',distinction:'The presence of model calls, conditional branches, or a framework named “agent” does not settle this. Look at who selects the next action in the actual system.',depth:'Real applications are often hybrids: a deterministic intake flow can invoke a bounded research agent, validate its output, and wait for approval. Keep controls in code even when task planning is model-directed.',measure:'Compare completion quality, failure rate, latency, and cost against the simpler workflow on the same tasks.',link:'single-agent',source:'https://www.anthropic.com/engineering/building-effective-agents',sourceName:'Building effective agents'},
 {title:'Reasoning effort or more tools?',question:'Is the bottleneck deciding well, or missing an observation?',baseline:'Give the model adequate evidence and useful tools, then tune reasoning effort against your actual tasks.',distinction:'Reasoning is important for planning, diagnosing failures, and revising a course of action. A dedicated reasoning model can improve those decisions, but “reasoning model” and “agent” describe different things.',depth:'More inference-time compute can increase latency and cost without resolving absent or wrong evidence. A request can reason extensively and never act; an agent can act through a tool loop with a model that exposes no separate reasoning setting. Inspect outcomes and observable actions rather than demanding private chain-of-thought.',measure:'Compare successful outcomes and tool choices across available reasoning settings. Record cost and time; do not use answer length as a proxy for reasoning quality.',link:'inference-time-reasoning',source:'https://ai.google.dev/gemini-api/docs/thinking',sourceName:'Thinking configuration and limitations'},
 {title:'One agent or several?',question:'Does decomposition reduce a real bottleneck?',baseline:'Try one capable, well-equipped agent first. Split work when subtasks can usefully proceed independently or need separate context and permissions.',distinction:'Parallel execution of a fixed plan is different from a lead agent deciding what to delegate. Separate agent instances do not guarantee independent mistakes.',depth:'Define task ownership, a handoff contract, the evidence a worker must return, and what happens on timeout or disagreement. Avoid letting every worker modify the same shared artifact. A reviewer using the same misleading sources can reinforce an error instead of catching it.',measure:'Include coordination overhead, duplicated searches, omitted findings, and synthesis errors in the comparison—not just wall-clock speed.',link:'orchestrator-workers',source:'https://www.anthropic.com/engineering/multi-agent-research-system',sourceName:'Multi-agent research system'},
 {title:'Structured output or verified truth?',question:'What does the downstream code actually need to trust?',baseline:'Define the output contract, validate it, and recompute things ordinary code can check.',distinction:'Valid JSON, schema compliance, factual accuracy, and permission to act are four separate properties. Passing one does not establish the others.',depth:'Plan for missing values, refusals, truncated responses, extra fields, and invalid tool arguments. A citation validator can establish that a source ID exists, while a separate check asks whether the source supports the claim. Automatic repairs must not invent missing facts.',measure:'Track format failures and factual errors separately. Keep a source-backed reference or a checkable external outcome for important fields.',link:'structured-output',source:'https://docs.ollama.com/capabilities/structured-outputs',sourceName:'Structured outputs in Ollama'},
 {title:'Tools, protocols, and authority',question:'What is the system allowed to do, and who enforces that?',baseline:'Expose the smallest useful tool set and validate arguments and authorization before execution.',distinction:'MCP standardizes a connection to tools and context. It does not turn a tool description into authorization, validate every business rule, or make retrieved instructions trustworthy.',depth:'Keep identity, scope, consent, and execution checks in the host application. Treat tool results and retrieved documents as potentially untrusted. A prompt-injection defense is stronger when a malicious instruction cannot acquire a capability the tool layer never granted.',measure:'Exercise denied tools, out-of-scope arguments, untrusted instructions, changed approvals, and uncertain external outcomes.',link:'mcp',source:'https://modelcontextprotocol.io/specification/2025-11-25/server/tools',sourceName:'MCP tool specification'},
 {title:'Long-running or always-on?',question:'Must work survive a session ending, a restart, or a quiet period?',baseline:'Save a durable checkpoint and define how a new run starts, resumes, stops, and asks for help.',distinction:'Long-horizon describes work spanning time. Always-on describes availability and triggers. Neither requires a team of agents or constant model generation.',depth:'A context summary is not the same as a durable task ledger or an external-action receipt. Preserve completed work, remaining work, blockers, relevant evidence, and a reproducible environment. A crash between an external effect and recording its receipt creates an ambiguous retry; resolve that ambiguity before repeating the action.',measure:'Test process interruption, stale checkpoints, replayed events, expired authority, and duplicate actions. Verify the actual artifact after resuming.',link:'agent-teammates',source:'https://www.anthropic.com/engineering/effective-harnesses-for-long-running-agents',sourceName:'Harnesses for long-running agents'},
 {title:'Evaluation, observation, and release',question:'What evidence would justify relying on this system?',baseline:'Define a representative test set and acceptance criteria, then inspect failures alongside aggregate scores.',distinction:'An evaluation grades task behavior or outcomes. A trace records how a run unfolded. Both are useful; neither replaces the other.',depth:'Include typical cases, difficult cases, and cases where the correct behavior is to refuse, abstain, or ask for help. Keep prompt-development cases separate from held-out evaluation. Repeat stochastic tasks, calibrate model graders against human judgment, and check final environment state rather than trusting a success message.',measure:'Report task success, unsafe or unsupported outcomes, latency, resource use, and uncertainty. Record the model, harness, tools, data, and configuration so comparisons are meaningful.',link:'evals',source:'https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents',sourceName:'Demystifying evaluations for agents'},
];
